import chai from 'chai';
import { sampleBookings } from './sample-bookings';
import { sampleRooms } from './sample-rooms';
import { sampleCustomers } from './sample-customers';
const expect = chai.expect;

describe('Customer interaction', function() {
  it('Customers should be able to select a date', function() {
    expect(true).to.equal(true);
  });

  it('Only available rooms should be returned given a specific date', function() {
    expect(true).to.equal(true);
  });

  it('Customers should be able to filter the list of available rooms by room type', function() {
    expect(true).to.equal(true);
  });

  it('Customers should be able to select a room', function() {
    expect(true).to.equal(true);
  });

  it('Customers should not be able to book a room if that room is unavailable', function() {
    expect(true).to.equal(true);
  });

  it('A message should be rendered if no room is available for the selected date', function() {
    expect(true).to.equal(true);
  });

  it('A message should be rendered if no room is available for the selected room type', function() {
    expect(true).to.equal(true);
  });
 
  it('Customers should be able to adjust the room search', function() {
    expect(true).to.equal(true);
  });

  it('', function() {
    expect(true).to.equal(true);
  });

  it('', function() {
    expect(true).to.equal(true);
  });

});